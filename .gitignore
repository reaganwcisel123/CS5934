# Cached raw source extracts (regenerated by `python src/build_dataset.py --refresh`).
# Kept out of git because upstream files (HRSA, USDA) are tens of MB each.
data/raw/*
!data/raw/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets (e.g. CENSUS_API_KEY) -- never commit
.env

# Live data pipeline dependencies.
# NOTE: pyproject.toml + uv.lock are the canonical, reproducible source of truth
# (run `uv sync`). This file is a pip fallback and mirrors [project.dependencies].
requests>=2.31      # HTTP calls to CDC Socrata, Census, HRSA, USDA APIs
pandas>=2.0         # tabular transforms and county-level joins
PyYAML>=6.0         # read the data_sources.yml catalog
openpyxl>=3.1       # read the USDA Food Access Atlas .xlsx workbook
python-dotenv>=1.0  # load .env locally (Render injects env vars directly)
